<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aperobox">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.example.aperobox.application.AperoBoxApplication"
        android:theme="@style/Theme.AperoBox">
        <activity android:name=".Activity.PanierView" />
        <activity android:name=".Activity.CommentaireView" />
        <activity android:name=".Activity.BoxView" />
        <activity android:name=".Activity.Options" />
        <activity android:name=".Activity.Inscription" />
        <activity android:name=".Activity.Connexion" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>